import { Cmdr } from "@rbxts/cmdr";

// Setup commands
// const folders = script.Parent as Folder;

Cmdr.RegisterDefaultCommands();

// Add folders for these
// Cmdr.RegisterCommandsIn(folders.FindFirstChild("commands") as Folder);
// Cmdr.RegisterTypesIn(folders.FindFirstChild("types") as Folder);

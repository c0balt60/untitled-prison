import { Cmdr } from "@rbxts/cmdr";

// Setup commands
const folders = script.Parent as Folder;

Cmdr.RegisterDefaultCommands();
// Add folders for these

// Cmdr.RegisterCommandsIn( <Folder>folders.FindFirstChild("commands") )
// Cmdr.RegisterTypesIn( <Folder>folders.FindFirstChild("types") )
